// Main packet services
#include "bktypes.h"

bool slotaNps(Byte *packet, Byte length);
bool slotaSps(Byte *packet, Byte length);
bool slotbNps(Byte *packet, Byte length);
bool slotbSps(Byte *packet, Byte length);
